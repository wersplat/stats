{
  "name": "stats",
  "version": "1.0.0",
  "description": "A static website to display game recaps",
  "main": "_worker.js",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && wrangler deploy",
    "dev": "wrangler dev"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
